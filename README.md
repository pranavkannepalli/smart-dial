# Smart Dial

This repo contains SwiftPM modules for the **Smart Productivity Dial**.

## Modules
- `DialCore`: Pure dial state + control loop (no UI)
- `DialUI`: SwiftUI dial UI

## Build
```bash
swift build
```
